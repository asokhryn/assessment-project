export interface ISideBarLinkList {
  text: string;
  icon: string;
  link: string;
  exact: boolean;
}

export interface ITextContentConfirm {
  text: string,
  cancelButton: string,
  confirmButton: string
}
